- title "Listado de Mermas"

#opciones
  %strong Filtrar por:
  %br
  = select_tag "tipo_merma", options_for_select({ "Fecha de vencimiento" => 0, "Producto dañado" => 1, "Autoconsumo" => 2, "Otra razon" => 3}, params[:tipo_merma]),:onchange => "document.location='/mermas?tipo_merma=' + this.value"

= render "shared/autocomplete", :objeto => @productos

- if @mermas.length == 0
  %p.empty_table No existen mermas ingresadas.
- else
  %table.main_table
    %th Producto
    %th Cantidad
    %th Motivo
    %th Fecha
    - @mermas.each do |merma|
      %tr
        %td= merma.producto.nombre
        %td= merma.cantidad
        %td= merma.tipo_merma
        %td= merma.updated_at
        %td= link_to "Editar", [:edit, merma]
        %td= link_to "Eliminar", merma, :method => :delete, :confirm =>'¿Está seguro de eliminar esta merma?'
%p= link_to "Nueva Merma", [:new,:merma], :class => "action_button radius"
