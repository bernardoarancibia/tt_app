- title "Listado de Notas"
- if @notas.length == 0
  %p.empty_table No existen notas ingresadas.
- else
  %table.main_table
    %th Vendedor
    %th Nota
    - @notas.each do |nota|
      %tr
        %td= "#{nota.vendedor.nombre} #{nota.vendedor.apellidos}"
        %td= link_to truncate(nota.texto, :length => 50, :omission => '...'), nota
        %td= distance_of_time_in_words(nota.created_at, Time.now, include_seconds = true)
        %td= link_to "Editar", [:edit, nota]
        %td= link_to "Eliminar", nota, :method => :delete, :confirm =>'¿Está seguro de eliminar esta nota?'
%p= link_to "Nueva Nota", [:new,:nota], :class => "action_button radius"
