!!!5
%html
  %head
    %title= yield(:head) || "Provisiones Frutras y Verduras Chusmisa"
    = stylesheet_link_tag "application"
    = javascript_include_tag :defaults, "jquery.autocomplete", "jquery.bigframe.min"
    = csrf_meta_tag

  %body
    #header
      = render "layouts/header"
      #loged
        = link_to "Cerrar sesión", :logout if session[:vendedor_id] || session[:cliente_id]
    #container
      %h2= yield(:head)
      = flash_helper
      = yield
    #footer
      = render "layouts/footer"

    // ELIMINAR EN PRODUCCION

    %br
    %hr
    = debug(session)
    = debug(params)
